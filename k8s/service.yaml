apiVersion: v1
kind: Service
metadata:
  name: fast-service
  namespace: default
spec:
  type: Nodeport
  selector:
    app: fastapi
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    nodeport: 30081
